# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env — the Anthropic key is server-side only (CLAUDE.md §4.1). Never commit it.
.env
.env*.local
.env.production

# vercel
.vercel

# typescript
*.tsbuildinfo

# Founder source documents. These live in ../tenderside-documentation/, outside the
# repo, because they include the monetization blueprint, investor vision document and
# executive summary. This entry is belt-and-braces: if a copy ever reappears here, it
# must not be committed. Git history is permanent and public repos are one toggle away.
/documentation/
